import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.relevamientovisualpps',
  appName: 'RelevamientoVisualPps',
  webDir: 'www'
};

export default config;
